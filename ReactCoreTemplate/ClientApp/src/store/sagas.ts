//import { searchSagaWatchers } from '@store/search';
import { fork } from 'redux-saga/effects';

// Register all your watchers
export function* rootSaga() {
    //yield fork(takeLatestWatcher('FETCH_MEMBERS_REQUEST', fetchMembers)), 
  //yield fork(searchSagaWatchers.fetchSearchParametersWatcher);
}
